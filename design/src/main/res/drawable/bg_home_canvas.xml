<?xml version="1.0" encoding="utf-8"?>
<!-- Home canvas: the themed window background plus a soft ambient light glow from the top-centre.
     Theme-safe (base reads the theme attr; the glow is a low-alpha white radial that reads as
     premium depth on dark and is barely-there on light). Redesign 1.0. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <!-- Material ?attr/colorBackground (not ?android:attr/...) so the TrueBlack overlay, which
             sets the custom colorBackground, repaints the canvas to #000000 on pure black. -->
        <color android:color="?attr/colorBackground" />
    </item>
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:centerX="0.5"
                android:centerY="0.14"
                android:endColor="#00FFFFFF"
                android:gradientRadius="760dp"
                android:startColor="?attr/homeGlowColor"
                android:type="radial" />
        </shape>
    </item>
</layer-list>
